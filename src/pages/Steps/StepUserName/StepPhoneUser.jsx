import React from "react";

const StepPhoneUser = ({ onNext }) => {
  return (
    <>
      <div>PhoneUser</div>
      <button onClick={onNext}>Next</button>
    </>
  );
};

export default StepPhoneUser;
